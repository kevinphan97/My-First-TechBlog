<div class="comments text-light">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_entry}}
    </div>
  </section>
  {{/each}}
</div>